# Policy Baseline

This folder contains active governance policy assignments.

## Implemented
- `security-baseline.bicep`
  - Allowed locations policy assignment
  - Required tags and values (`environment`, `project`, `managedBy`)

## Notes
- Built-in policy definition IDs are referenced directly.
- If your tenant uses custom initiatives or different standards, replace assignment targets accordingly.
